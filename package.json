{
  "name": "pawrescue-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "build:all": "npm run build --prefix client && npm run build --prefix server && npm run build --prefix ngo-client",
    "install:all": "npm install --prefix client && npm install --prefix server && npm install --prefix ngo-client",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}